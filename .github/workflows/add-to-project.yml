name: Add issues to Data Source Plugins team project
on:
  issues:
    types:
      - opened
  pull_request:
    types:
      - opened

jobs:
  add-to-project:
    permissions:
      contents: read
      id-token: write
    name: Add issue to project
    runs-on: ubuntu-latest
    steps:
      - id: get-github-token
        uses: grafana/shared-workflows/actions/create-github-app-token@795f748a236f9de024b7514efc9a208456e7e468 # create-github-app-token/v0.3.0
        with:
          github_app: grafana-oss-big-tent
      - name: Add to project
        uses: actions/add-to-project@8b5d8dd2895c251002b427e4688a6115b5ed2f09 # main
        with:
          project-url: https://github.com/orgs/grafana/projects/1085
          github-token: ${{ steps.get-github-token.outputs.token }}
